<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/android:windowBackground">

    <!-- Top AppBar -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/appBarBackground"
        app:elevation="0dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                app:theme="@style/ThemeOverlay.App.Toolbar">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <!-- Clickable App Name (Triggers Help Dialog) -->
                    <TextView
                        android:id="@+id/tvAppTitle"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_weight="1"
                        android:background="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true"
                        android:gravity="center_vertical"
                        android:paddingStart="4dp"
                        android:paddingEnd="8dp"
                        android:text="@string/app_name"
                        android:textColor="?attr/appBarTitleColor"
                        android:textSize="20sp"
                        android:textStyle="bold" />

                    <!-- WhatsApp / WA Business Source Switcher Button -->
                    <Button
                        android:id="@+id/btnAppSource"
                        style="@style/Widget.AppCompat.Button.Borderless"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:layout_marginEnd="4dp"
                        android:background="?attr/appBarSourceButtonBackground"
                        android:paddingStart="10dp"
                        android:paddingEnd="10dp"
                        android:text="@string/source_whatsapp"
                        android:textAllCaps="true"
                        android:textColor="?attr/appBarSourceButtonTextColor"
                        android:textSize="12sp"
                        android:textStyle="bold" />

                </LinearLayout>

            </androidx.appcompat.widget.Toolbar>

            <!-- Contextual Action Bar for Multi-Select -->
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/selectionToolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:visibility="gone"
                app:contentInsetLeft="4dp"
                app:contentInsetStart="4dp"
                tools:visibility="visible">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageButton
                        android:id="@+id/btnCloseSelection"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/cancel"
                        android:padding="10dp"
                        android:src="@drawable/ic_close"
                        app:tint="?attr/appBarIconTint" />

                    <!-- Clickable Selected Count text (triggers Select All) -->
                    <TextView
                        android:id="@+id/tvSelectedCount"
                        android:layout_width="0dp"
                        android:layout_height="match_parent"
                        android:layout_marginStart="4dp"
                        android:layout_weight="1"
                        android:background="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true"
                        android:gravity="center_vertical"
                        android:paddingStart="4dp"
                        android:paddingEnd="4dp"
                        android:textColor="?attr/appBarTitleColor"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        tools:text="1 selected" />

                    <ImageButton
                        android:id="@+id/btnSelectAll"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/action_refresh"
                        android:padding="10dp"
                        android:src="@drawable/ic_check_circle"
                        app:tint="?attr/appBarIconTint" />

                    <ImageButton
                        android:id="@+id/btnBatchSave"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/action_save"
                        android:padding="10dp"
                        android:src="@drawable/ic_file_download"
                        app:tint="?attr/appBarIconTint" />

                    <ImageButton
                        android:id="@+id/btnBatchShare"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/action_share"
                        android:padding="10dp"
                        android:src="@drawable/ic_share"
                        app:tint="?attr/appBarIconTint" />

                    <ImageButton
                        android:id="@+id/btnBatchDelete"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:layout_marginEnd="4dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/action_delete"
                        android:padding="10dp"
                        android:src="@drawable/ic_delete"
                        android:visibility="gone"
                        app:tint="?attr/appBarIconTint"
                        tools:visibility="visible" />

                </LinearLayout>

            </androidx.appcompat.widget.Toolbar>

        </FrameLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Main Content Container with ViewPager2 and BottomNavigationView -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- ViewPager2 for tab fragments -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Bottom Navigation View with Native M3 styling and M2 fallback -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/bottomNavBackground"
            app:backgroundTint="?attr/bottomNavBackground"
            app:elevation="8dp"
            app:itemIconTint="@color/bottom_nav_item_color"
            app:itemTextColor="@color/bottom_nav_item_color"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_navigation_menu" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>